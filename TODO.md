# TODO

- [x] Read existing Vue scaffold files (package.json, App.vue, main.ts, index.html, README)
- [x] Implement Dame game UI + logic in `src/App.vue` (2P + vs CPU)
- [x] Add interactive animated background on the start/menu screen (canvas particles)
- [ ] Run `npm install` (if needed)
- [ ] Run `npm run dev` and manually test:
  - [ ] 2P move selection + legal move enforcement
  - [ ] mandatory captures + multi-jumps
  - [ ] king promotion
  - [ ] vs-computer turn-taking + basic AI playability
  - [ ] game-over detection and restart

